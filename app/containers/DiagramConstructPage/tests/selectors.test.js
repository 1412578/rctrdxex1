// import { fromJS } from 'immutable';
// import { selectDiagramConstructPageDomain } from '../selectors';

describe('selectDiagramConstructPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
